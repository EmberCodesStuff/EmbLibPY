[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "emblibpy"
version = "0.1.0"
description = "i made this library for fun, its got silly cat stuff and some math stuff LWL"
requires-python = ">=3.10"

[tool.setuptools.package-data]
embLibPY = ["Audio/*.wav"]